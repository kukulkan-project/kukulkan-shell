package mx.dads.infotec.archetype;

import mx.dads.infotec.${project.id}.${grammar.name}BaseVisitor;

/**
 * 
 * @author ${project.id?cap_first} generated
 *
 */
public class ${grammar.name}CustomVisitor extends ${grammar.name}BaseVisitor<Object> {

}
